# 변경 이력

이 파일은 릴리즈 단위의 변경 사항을 기록한다.
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다 — 기능 추가는 minor,
버그픽스는 patch, 저장된 기록의 형식이 깨지는 변경은 major다.

## [1.0.0] — 2026-09-09

리드미컬 첫 릴리즈. 메트로놈에 맞춰 탭하면 타이밍 정확도를 바로 판정해 준다.

### 기능

- **패턴 5종** — 4분음표 · 8분음표 · 셋잇단 · 싱커페이션 · 8비트 믹스
- **연습 화면** — 메트로놈 재생(패턴별 BPM 범위, 셋잇단은 60~100), 비트 인디케이터,
  탭 입력(터치 · 스페이스바)
- **타이밍 판정** — Perfect(±50ms) / Good(±120ms) / Miss. 인접 간격이 좁으면
  허용 오차를 동적으로 좁혀 이중 매칭을 막는다
- **결과 화면** — 정확도, 판정 분포, 최고 기록 갱신 배지, 다시하기
- **로컬 기록** — 패턴별 최고 정확도 저장(localStorage), 새로고침 후에도 유지
- **지연 보정** — 기기별 오디오 지연 오프셋 슬라이더(−200 ~ +200ms)

### 제약

- 회원가입 · 서버 저장 없음. 기록은 브라우저에만 남는다
- 라이트 모드 없음(야간 연습을 가정한 다크 배경 고정)
- 마이크 입력 판정 없음 — 탭 입력만 판정한다

### 알려진 사항

- **iOS Safari · Android Chrome 실기기 검증을 마치지 못한 채 릴리즈한다.**
  자동 검증은 데스크톱 Chrome 범위이며, 실기기의 오디오 재생 여부와 터치 입력
  지연은 확인되지 않았다. 감수한 위험이며 근거는 결정 로그에 있다.
  문제가 보고되면 운영 방침의 P0 · P1 기준으로 대응한다
- 사파리 프라이빗 모드 등 localStorage를 쓸 수 없는 환경에서는 기록만 비활성되고
  연습은 정상 동작한다
- 기기별 오디오 지연은 자동 보정하지 않는다. 판정이 밀린다고 느껴지면
  설정에서 지연 보정을 조절한다
- 사용 지표를 수집하지 않는다. 계측 도구는 다음 패치에서 선정한다
